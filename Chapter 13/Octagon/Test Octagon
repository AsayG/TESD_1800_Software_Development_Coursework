package triangle;
import java.lang.Cloneable;
import java.util.Scanner;

public class testTriangle {
	public static void main(String[] args) throws CloneNotSupportedException{
		Scanner scan = new Scanner(System.in);
		
		Triangle octagon1 = new Triangle();
		GeometricObject geometricObject = new GeometricObject();
		
		System.out.print("Enter side lengths of Object:");
		octagon1.side1 = scan.nextDouble();
		
		Triangle octagon2 =  (Triangle) octagon1;
		
		
		
		System.out.println("Area is:" + octagon1.getArea());
		
		int result = (octagon1.compareTo(octagon2));
		if (result == 1)
			System.out.println("Octagon is greather than its clone.");
		else if (result == -1)
			System.out.println("Octagon is less than its clone."); 
		else
			System.out.println("Octagon is equal to its clone.");
		
		
		
	}
}
